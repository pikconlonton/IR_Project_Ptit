[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "scifact-retrieval"
version = "0.1.0"
description = "BM25 retrieval + MMR reranking baseline for SciFact"
requires-python = ">=3.9"

[tool.pytest.ini_options]
testpaths = ["tests"]
